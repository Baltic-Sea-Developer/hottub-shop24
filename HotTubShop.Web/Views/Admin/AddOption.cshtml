@model AdminOptionEditViewModel
@{
    ViewData["Title"] = "Option anlegen";
}

<h1 class="h3 mb-3">Option anlegen</h1>
@if (TempData["AdminError"] is string adminError && !string.IsNullOrWhiteSpace(adminError))
{
    <div class="alert alert-danger">@adminError</div>
}
<p class="text-secondary">Bestehende Gruppe wählen oder neue Gruppe anlegen. Jede Gruppe wird im Shop als Einzel-Auswahl dargestellt.</p>
<partial name="_OptionForm" model="Model" />
